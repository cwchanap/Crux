#!/usr/bin/env python3
"""Freeze one already-working external separator runtime into a lock."""

from __future__ import annotations

from src.cli.freeze_separator_runtime import main

if __name__ == "__main__":
    raise SystemExit(main())
